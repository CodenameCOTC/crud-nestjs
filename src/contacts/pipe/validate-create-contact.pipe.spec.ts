import { ValidateContactPipe } from './validate-contact-dto';

describe('ValidateContactPipe', () => {
  it('should be defined', () => {
    expect(new ValidateContactPipe()).toBeDefined();
  });
});
